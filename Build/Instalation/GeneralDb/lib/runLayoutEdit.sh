#! /bin/sh

    libdir="%INSTALL_PATH/lib"
    
    %JAVA_HOME/bin/java -jar "${libdir}/runLayouteditor.jar" 

    ### note: Replace <INSTALLDIR> with the actual install directory     
    ### note: Replace <DBJAR> with the actual Jar holding the JDBC 
