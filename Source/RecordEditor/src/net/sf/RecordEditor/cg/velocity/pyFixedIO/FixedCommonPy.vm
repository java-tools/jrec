#
#  Generated by the RecordEditor (https://sourceforge.net/projects/record-editor/)
#  on the ${generateOptions.getTemplateDtls().currentDateTime}
#
#macro (fixedClassPy)
class ${schemaDef.getClassName()} :                                               
    def assign(self, line):
#*  *##foreach($f in ${r.getFields()})
#*      *##set ( $p = ${f.fieldDetail.getPos()} - 1 ) 
#*      *##set ( $ep = ${p} + ${f.fieldDetail.getLen()} ) 
#*      *##if (${f.isNumeric()})	
        self.${f.javaName} = line[${p}:${ep}].strip()
#*      *##else	
        self.${f.javaName} = line[${p}:${ep}]
#*      *##end
#*  *##end

    def toLine(self):
        return (
#*  *##set ( $pos = 1)
#*  *##set ( $sep = ' ')
#*  *##foreach($f in ${r.getFields()})
#*      *##if (${f.fieldDetail.getPos()} >= $pos )		
#*          *##if (${f.fieldDetail.getPos()} > $pos )	
#*              *##set ( $len = ${f.fieldDetail.getPos()} - $pos )
#*              *##if (${len} == 1 )	
        	$sep ' '
#*              *##else
        	$sep ' '.ljust(${f.fieldDetail.getLen()}, ' ')
#*              *##end
#*              *##set ( $sep = '+')
#*          *##end
#*          *##if (${f.isNumeric()})	
        	$sep str(self.${f.javaName}).rjust(${f.fieldDetail.getLen()}, ' ')
#*          *##else	
        	$sep self.${f.javaName}.ljust(${f.fieldDetail.getLen()}, ' ')
#*          *##end
#*          *##set ( $sep = '+')
#*          *##set ( $pos = $pos + ${f.fieldDetail.getLen()})
#*      *##end
#*  *##end
               )


    def __init__(self, line = None):
        if line is None:
#*  *##foreach($f in ${r.getFields()}) 
#*      *##if (${f.isNumeric()})	
            self.${f.javaName} = 0
#*      *##else	
            self.${f.javaName} = ""
#*      *##end
#*  *##end
        else:
            self.assign(line)
#end

